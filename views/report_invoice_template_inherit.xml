<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Heredar la vista del reporte de facturas de l10n_cl -->
    <template id="report_invoice_inherit" inherit_id="l10n_cl.report_invoice_document">

        <!-- Modificar el borde de la caja -->
        <xpath expr="//div[contains(@class, 'border-dark')]" position="attributes">
            <attribute name="class">border border-4</attribute>
            <attribute name="style">border-color: red;</attribute>
        </xpath>

        <!-- Cambiar "VAT" por "IVA" en el subtotal de impuestos -->
        <xpath expr="//td[contains(text(),'VAT')]" position="replace">
            <td t-out="'IVA %s' % subtotal_amounts['vat_percent']"/>
        </xpath>

    </template>
</odoo>
